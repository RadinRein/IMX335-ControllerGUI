# Core dependencies
PyQt6>=6.0
opencv-python>=4.5
numpy>=1.20

# Optional but recommended for Linux (to enumerate V4L2 devices)
# You can install system-wide: sudo apt install v4l-utils
# python-v4l2capture is an alternative package if you want to expand native capture features
